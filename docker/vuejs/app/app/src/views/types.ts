export interface SignUp {
  signup: boolean;
}

export interface SignIn {
  signin: boolean;
}

export interface Refresh {
  refresh: boolean;
}

export interface FreshSignIn {
  fresh_signin: boolean;
}

export interface SignOut {
  signout: boolean;
}
